#pragma once
// Stub for Arduino_GFX compatibility on Arduino ESP32 2.x (IDF 4.x).
// esp32-hal-periman.h is a 3.x-only header; the SPI bus driver includes it
// but does not call any functions from it when built on 2.x targets.
